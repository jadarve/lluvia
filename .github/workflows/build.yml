name: build

on: [push]

jobs:
  # linux_cpp:
  #   name: Linux C++
  #   runs-on: ubuntu-latest
  #   container:
  #     image: jadarve/lluvia:latest
  #   steps:
  #   - uses: actions/checkout@v1
  #   - name: Build all
  #     run: bazel build //lluvia/...

  # linux:
  #   name: Linux build
  #   runs-on: ubuntu-20.04
  #   steps:
  #   - name: Install Python dependencies
  #     run: |
  #       sudo apt install -y python3.8-dev
  #       pip3 install --no-cache-dir cython numpy pytest jinja2 markupsafe
  #   - name: Install Vulkan SDK
  #     run: |
  #       wget -qO - https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo apt-key add -
  #       sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-1.2.176-focal.list https://packages.lunarg.com/vulkan/1.2.176/lunarg-vulkan-1.2.176-focal.list
  #       sudo apt update
  #       sudo apt install -y vulkan-sdk
  #   - uses: actions/checkout@v1
  #   - name: Build
  #     run: bazel build //lluvia/...
  
  windows:
    name: Windows build
    runs-on: windows-2019
    steps:
    - name: Install tools
      run: |
        choco install wget
    - name: Install Python dependencies
      run: pip3 install cython numpy pytest jinja2 markupsafe
    - name: Install Vulkan SDK
      run: |
        wget -O VulkanSDK-Installer.exe https://sdk.lunarg.com/sdk/download/1.2.176.1/windows/VulkanSDK-1.2.176.1-Installer.exe
        ./VulkanSDK-Installer.exe /S
    - uses: actions/checkout@v1
    - name: Build
      run: bazel build //lluvia/cpp/...
      